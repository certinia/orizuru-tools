	public class {{qualifiedName}} extends OrizuruTransport // Transport extension for use when sending data to Orizuru Heroku apis
	{
		{{getSetParams}}
		public {{qualifiedName}}({{constructorParams}}) {
			{{getSetParamAssignments}}
		}
	}
