	public class {{qualifiedName}} // Internal class defining a type within an Orizuru Avro transport schema, used within transport extensions
	{

		{{getSetParams}}
		public {{qualifiedName}}({{constructorParams}}) {
			
			{{getSetParamAssignments}}
		}

	}
